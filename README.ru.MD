# ⚡ SeamlessRouter

[![TypeScript](https://img.shields.io/badge/typescript-%233178C6.svg?style=for-the-badge&logo=typescript&logoColor=white)](https://www.typescriptlang.org/)
[![JavaScript](https://img.shields.io/badge/javascript-%23323330.svg?style=for-the-badge&logo=javascript&logoColor=%23F7DF1E)](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
[![Zero Config](https://img.shields.io/badge/zero_config-2E8B57.svg?style=for-the-badge&logo=simpleanalytics&logoColor=white)](#)
[![Lightweight](https://img.shields.io/badge/lightweight-FF6B35.svg?style=for-the-badge&logo=linuxcontainers&logoColor=white)](#)
[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg?style=for-the-badge)](./LICENSE)

---

### 🧩 Что это за роутер?

**SeamlessRouter** — ультра-легковесный роутер для AJAX навигации, который делает переходы между страницами плавными без полной перезагрузки страницы.

🌐 **Работает с обычными HTML-файлами и CMS** (MODX, WordPress, Bitrix и другими)  
⚡ **Самый простой способ ускорить ваш сайт** — всего 1 файл, 0 конфигурации  
🧹 **Автоматически чистит мусор** — таймеры, слушатели событий и старые скрипты  
🔌 **Никаких зависимостей** — чистый TypeScript/JavaScript  
🎯 **Полный контроль** — специальные атрибуты для управления поведением скриптов

**Ключевая особенность**: роутер работает на любой странице, даже если вы зашли напрямую по ссылке (например, сразу на `/blog/`).

---

### 📦 Установка и использование

```bash
# Установите зависимости (только для разработки)
pnpm install

# Соберите проект
pnpm build
```

После сборки возьмите файл из `dist/` и подключите его на свой сайт **обязательно с атрибутами**:

```html
<script data-keep data-skip src="seamless-router.js"></script>
```

**Атрибуты подключения**:
- `data-keep` — предотвращает удаление скрипта при навигации
- `data-skip` — предотвращает повторное выполнение скрипта при загрузке новых страниц

**Всё!** Роутер автоматически инициализируется и начинает работать. Никаких настроек, вызовов `init()` или конфигурации.

---

### 🚀 Как это работает

1. **Перехватывает клики** по ссылкам (`<a href="...">`) и кнопкам с `data-router-link`
2. **Загружает новую страницу** через `fetch()` без перезагрузки
3. **Обновляет только изменённые части**: заголовок, мета-теги, контент
4. **Автоматически очищает** скрипты, таймеры и слушатели предыдущей страницы
5. **Если что-то пошло не так** — автоматический откат к обычной перезагрузке

---

### ⚙️ Контроль над скриптами с помощью data-атрибутов

Роутер предоставляет специальные атрибуты для полного контроля над поведением скриптов:

```html
<!-- data-keep: скрипт НЕ удаляется при переходе на новую страницу -->
<script data-keep src="shared-library.js"></script>

<!-- data-skip: скрипт НЕ выполняется при загрузке новой страницы -->
<script data-skip src="analytics.js"></script>

<!-- data-reload: скрипт выполняется КАЖДЫЙ РАЗ, даже если уже был в кэше -->
<script data-reload src="dynamic-widget.js"></script>

<!-- Комбинации: не удалять И не выполнять -->
<script data-keep data-skip src="seamless-router.js"></script>
```

---

### 🛡️ **ВАЖНОЕ ТРЕБОВАНИЕ ДЛЯ СКРИПТОВ**

Для полной изоляции и предотвращения конфликтов, **все ваши скрипты должны быть обёрнуты в IIFE** (Immediately Invoked Function Expression):

```javascript
// ✅ ПРАВИЛЬНО — скрипт в IIFE формате
(function() {
    'use strict';
    // Весь код вашей страницы здесь
    let slider = new Slider();
    window.myConfig = { /* ... */ }; // Даже window.xxx безопасно
})();

// ❌ НЕПРАВИЛЬНО — глобальные переменные
var slider = new Slider(); // Вызовет конфликты между страницами!
```

#### Почему это важно?
- **Изоляция**: переменные одной страницы не конфликтуют с другой
- **Безопасность**: никаких утечек памяти
- **Простота**: не нужно думать об уникальности имён переменных
- **Совместимость**: если скрипты в IIFE, роутер работает идеально

**Роутер НЕ требует** оборачивать скрипты в IIFE автоматически — это ответственность разработчика.

---

### 🧠 Структура проекта

```
SeamlessRouter/
├── src/
│   ├── core/
│   │   ├── Router/          # Логика навигации
│   │   ├── Sandbox/         # Песочница для изоляции скриптов
│   │   └── utils/           # Вспомогательные функции
│   └── index.ts             # Точка входа
├── dist/
│   └── seamless-router.js   # Готовый к использованию файл
├── examples/                # Примеры использования
└── README.md
```

---

### 🎯 Преимущества перед аналогами

| Особенность | SeamlessRouter | Другие роутеры |
|------------|----------------|----------------|
| Размер | **~5KB** (после сборки) | 50-200KB |
| Зависимости | **0** | React/Vue/Angular |
| Конфигурация | **Не требуется** | JSON, объекты, настройки |
| Совместимость | **Любой HTML/CMS** | Только SPA |
| Инициализация | **Автоматическая** | Ручной вызов `init()` |
| Требования | **Только IIFE** | Архитектура, сборка, фреймворк |

---

### 📖 Пример использования

1. **Подключите роутер** на все страницы сайта с правильными атрибутами:

```html
<html>
<head>
    <title>Мой сайт</title>
</head>
<body>
    <nav>
        <a href="/">Главная</a>
        <a href="/blog/">Блог</a>
        <a href="/about/">О нас</a>
    </nav>
    
    <!-- Общие библиотеки, которые нужны на всех страницах -->
    <script data-keep src="shared-library.js"></script>
    
    <!-- Сам роутер - не удаляем и не перезапускаем -->
    <script data-keep data-skip src="seamless-router.js"></script>
    
    <script>
    // Все скрипты страницы в IIFE
    (function() {
        'use strict';
        console.log('Эта страница работает с SeamlessRouter!');
        
        // Этот код не конфликтует с кодом других страниц
        let pageData = { title: 'Главная' };
        window.pageData = pageData; // Можно писать в window
    })();
    </script>
</body>
</html>
```

2. **Создайте страницу `/blog/index.html`**:

```html
<!-- Виджет, который нужно обновлять каждый раз -->
<script data-reload src="comments-widget.js"></script>

<script>
// Блог тоже в IIFE
(function() {
    'use strict';
    console.log('Загружен блог!');
    
    // Можно использовать те же имена переменных
    let pageData = { title: 'Блог' }; // Не конфликтует с главной
    window.pageData = pageData;
})();
</script>
```

3. **Наслаждайтесь** быстрыми переходами без перезагрузки!

---

### 🚧 Планы на будущее

* **Предзагрузка страниц** — загрузка следующей страницы при наведении на ссылку
* **Интеллектуальное кэширование** — предсказание следующих страниц
* **Офлайн-режим** — работа с кэшированными страницами без интернета
* **Расширенные анимации** — плавные переходы между страницами

---

### 🤝 Вклад в проект

PR, улучшения и идеи приветствуются!  
Перед PR убедитесь, что проект собран и проверен.

---

### 🪪 Лицензия

Проект распространяется под лицензией [MIT](./LICENSE).  
Создано с ❤️ для скорости и плавности навигации.