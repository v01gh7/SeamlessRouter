# План реализации расширенных функций роутера

## 1. Goal
Реализовать четыре расширенные функции для SPA-роутера:
1. **Предзагрузка страниц** — загрузка следующей страницы при наведении на ссылку
2. **Интеллектуальное кэширование** — предсказание следующих страниц на основе поведения пользователя
3. **Офлайн-режим** — работа с кэшированными страницами без интернета
4. **Расширенные анимации** — плавные переходы между страницами

## 2. Context
Проект представляет собой SPA-роутер с текущей базовой функциональностью:
- Обработка кликов по ссылкам и кнопкам с `data-router-link`
- Динамическая загрузка страниц через `fetch`
- Обновление `head` и `body` без перезагрузки страницы
- Управление скриптами через sandbox
- Наблюдение за изменениями DOM через MutationObserver

Текущая архитектура:
- `Router` — основной класс
- `attachRouteListeners` — обработка кликов
- `navigation` — навигация и обновление страницы
- `loadPage` — загрузка страниц
- `cache.ts` — пустой файл (заготовка для кэширования)
- `dom.ts` — пустой файл (заготовка для DOM-операций)

## 3. Assumptions
1. Пользователь использует современные браузеры с поддержкой:
   - Service Worker API
   - Intersection Observer API
   - CSS Transitions/Animations
   - WeakMap/WeakSet
2. Все страницы сайта находятся на одном домене (same-origin)
3. Размер кэша будет ограничен (например, 50 МБ)
4. Анимации должны быть отключаемыми для пользователей с `prefers-reduced-motion`
5. Предзагрузка не должна блокировать основной поток

## 4. Constraints
1. **Производительность**: Предзагрузка не должна замедлять основной интерфейс
2. **Память**: Кэш должен иметь разумные ограничения
3. **Совместимость**: Должна сохраняться работа без JavaScript
4. **SEO**: Должны корректно работать мета-теги и история навигации
5. **Доступность**: Анимации должны учитывать `prefers-reduced-motion`

## 5. Non-Goals
1. Поддержка старых браузеров (IE11 и ниже)
2. Кэширование медиа-файлов (только HTML)
3. Предзагрузка внешних ссылок
4. Сложные ML-алгоритмы для предсказания (только простые эвристики)
5. Полноценный PWA с установкой на рабочий стол

## 6. Proposed Solution

### 6.1 Предзагрузка страниц (Hover Prefetch)
**Механизм**: Добавить обработчики `mouseenter` и `touchstart` к ссылкам
**Логика**: При наведении/тапе начинать загрузку страницы в фоне
**Отмена**: При `mouseleave` до завершения загрузки — отменять запрос
**Приоритет**: Низкий приоритет (`fetch(url, {priority: 'low'})`)

### 6.2 Интеллектуальное кэширование
**Стратегия**: LRU (Least Recently Used) кэш с простыми эвристиками
**Предсказание**: 
   - На ПК: загружать все первые страницы верхнего уровня (каталог, новости и т.д.)
   - При переходе в категорию с навигацией: загружать +2 следующие страницы
   - На мобилке: проверять размер экрана, загружать только первые страницы
**Хранение**: Использовать `Cache API` через Service Worker
**Ограничения**: 30 МБ, важные страницы (главная, контакты) всегда в кэше
**Проверка обновлений**: По `last-modified` тегу или хэшу контента

### 6.3 Офлайн-режим
**Service Worker**: Регистрация SW для кэширования
**Стратегия кэширования**: `NetworkFirst` с fallback на кэш
**Офлайн-страница**: Заглушка "интернета нет" при отсутствии сети и кэша
**Кэширование**: Только HTML, стили, скрипты. Видео/картинки не сохраняем

### 6.4 Расширенные анимации
**Подход**: CSS Transitions + JavaScript для контроля
**Типы анимаций**: Влево/вправо/вверх/вниз, схлопывание, по диагонали
**Контроль**: Классы для управления анимациями
**Доступность**: Учет `prefers-reduced-motion`
**Планы**: Позже добавить поддержку через mask и RTL (Right-to-Left)

## 7. Step-by-Step Plan

### Фаза 1: Подготовка инфраструктуры (2 дня)
1. **Создать систему конфигурации**
   - Файл `config.ts` с настройками кэша, анимаций, предзагрузки
   - Возможность отключения функций через конфиг

2. **Расширить Router класс**
   - Добавить методы для управления кэшем
   - Добавить обработчики событий для анимаций
   - Создать систему событий (EventEmitter)

3. **Создать утилиты для работы с Cache API**
   - `CacheManager` класс в `src/core/Cache/`
   - Методы: `set`, `get`, `delete`, `clear`, `has`

### Фаза 2: Предзагрузка страниц и улучшение навигации (2 дня)
4. **Модифицировать `attachRouteListeners.ts`**
   - Добавить обработчики `mouseenter` и `touchstart` для предзагрузки
   - Реализовать логику отмены загрузки при `mouseleave`
   - Добавить таймауты для предотвращения ложных срабатываний
   - Добавить поддержку `data-no-routing` атрибута
     - Если атрибут на родителе — игнорировать все дочерние элементы
     - Если атрибут на самой ссылке/кнопке — игнорировать при клике

5. **Создать `PrefetchManager`**
   - Управление очередью загрузок
   - Приоритизация запросов
   - Ограничение параллельных загрузок

6. **Интеграция с навигацией**
   - Проверка кэша перед загрузкой
   - Использование предзагруженных данных

### Фаза 3: Интеллектуальное кэширование (2 дня)
7. **Реализовать `IntelligentCache`**
   - Сбор статистики переходов
   - Алгоритм предсказания следующих страниц
   - Автоматическая предзагрузка предсказанных страниц

8. **Интеграция с Service Worker**
   - Регистрация SW
   - Стратегии кэширования
   - Фоновое обновление кэша

9. **Создать панель мониторинга кэша** (опционально)
   - Отладка в dev-режиме
   - Статистика попаданий/промахов

### Фаза 4: Офлайн-режим (1.5 дня)
10. **Реализовать Service Worker**
    - `sw.js` в корне проекта
    - Обработка fetch-событий
    - Кэширование статических ресурсов

11. **Офлайн-детекция**
    - Мониторинг состояния сети
    - Показ офлайн-индикатора
    - Очередь запросов для синхронизации

12. **Офлайн-страница**
    - Кастомный HTML для offline
    - Возможность повтора запроса

### Фаза 5: Расширенные анимации (1.5 дня)
13. **Создать `AnimationManager`**
    - Управление CSS-классами
    - Последовательность анимаций
    - Обработка прерываний

14. **Модифицировать `navigation.ts`**
    - Добавить хуки для анимаций
    - Асинхронное выполнение переходов
    - Обработка ошибок во время анимаций

15. **CSS-анимации**
    - Базовые стили переходов
    - Классы для кастомных анимаций
    - Поддержка `prefers-reduced-motion`

### Фаза 6: Интеграция и тестирование (1.5 дня)
16. **Интеграция всех компонентов**
    - Настройка зависимостей
    - Тестирование взаимодействия

17. **Тестирование**
    - Unit-тесты для новых классов
    - E2E-тесты навигации
    - Тестирование офлайн-режима

18. **Документация**
    - README с примерами использования
    - Конфигурационные опции
    - Примеры анимаций

## 8. Risks & Trade-offs

### Риски:
1. **Производительность**: Предзагрузка может потреблять трафик
   - **Мит**: Использовать `priority: 'low'` и отмену при `mouseleave`

2. **Память**: Кэш может расти бесконтрольно
   - **Мит**: LRU с ограничениями и периодическая очистка

3. **Сложность**: Увеличение кодовой базы
   - **Мит**: Модульная архитектура, опциональные функции

4. **Совместимость**: Service Worker требует HTTPS
   - **Мит**: Предусмотреть fallback для HTTP

### Компромиссы:
1. **Точность предсказания vs Простота**: Использовать простые эвристики вместо ML
2. **Свежесть данных vs Офлайн-работа**: `NetworkFirst` с fallback на кэш
3. **Плавность анимаций vs Производительность**: Использовать `will-change` и hardware acceleration

## 9. Validation Criteria

### Функциональные:
1. ✅ При наведении на ссылку страница загружается в фоне
2. ✅ При клике на предзагруженную ссылку — мгновенный переход
3. ✅ Кэш сохраняется между сессиями
4. ✅ При офлайне показываются кэшированные страницы
5. ✅ Анимации плавные и отключаемы

### Нефункциональные:
1. ⏱️ Время перехода < 100мс для кэшированных страниц
2. 📊 Использование памяти < 100MB
3. 📶 Работа при нестабильном соединении
4. ♿ Поддержка accessibility требований

## 10. Rollback / Recovery Plan

### Откат изменений:
1. **Поэтапный откат**: Каждую фичу можно отключать через конфиг
2. **Фолбэк**: При ошибках — возврат к текущей реализации
3. **A/B тестирование**: Включение фич для части пользователей

### Восстановление:
1. **Бэкап конфига**: Сохранение старой версии конфигурации
2. **Миграции данных**: Скрипты для очистки кэша при откате
3. **Мониторинг**: Логирование ошибок для быстрого выявления проблем

## 11. Open Questions

1. **Лицензирование**: Если они необходимы
   - **Ответ**: Cache API не требует дополнительных лицензий, это стандартный браузерный API

2. **Квоты браузера**: Какие ограничения по кэшу у разных браузеров?
   - **Ответ**: Ограничение 30 МБ, LRU стратегия. Главная, контакты и другие важные страницы всегда в кэше. Проверка `last-modified` тега перед повторным кэшированием

3. **Пользовательский контент**: Как обрабатывать страницы с динамическим контентом?
   - **Ответ**: Кэшируем только HTML, стили, скрипты. Видео/картинки не сохраняем. Если в кэше нет — показываем заглушку "интернета нет"

4. **Аналитика**: Собирать ли статистику использования для улучшения предсказаний?
   - **Ответ**: Без сложной аналитики. Простая логика: если пользователь на ПК — загружать все первые страницы верхнего уровня (каталог, новости и т.д.). Если перешел в категорию с навигацией — загружать +2 страницы. На мобилке проверять размер экрана и загружать только первые страницы

5. **Конфиденциальность**: Хранение истории навигации — нужно ли уведомление пользователя?
   - **Ответ**: Данные хранятся только в браузере пользователя, никуда не передаются. Если не обязательно по закону — не делать уведомление

6. **Интеграция с фреймворками**: Как будет работать с React/Vue компонентами?
   - **Ответ**: Не нужно интеграции с фреймворками. Роутер самодостаточный и создан не для них

7. **Тестирование Service Worker**: Какие инструменты использовать для тестирования SW?
   - **Ответ**: Будем использовать стандартные тесты, инструменты выберем позже

8. **Политика кэширования**: Как определить TTL для разных типов страниц?
   - **Ответ**: TTL по `last-modified` тегу. Если нет тега — по хэшу контента. Перед проверкой записываем размер body и каждого скрипта. Fallback заголовок для принудительного обновления

9. **Интернационализация**: Поддержка RTL-анимаций?
   - **Ответ**: RTL = Right-to-Left (арабские, иврит и т.д.). Сначала обычные анимации (влево/вправо/вверх/вниз, схлопывание, по диагонали), потом добавим поддержку через mask. Позже можно расширить

10. **Безопасность**: Проверка загружаемого HTML на XSS-уязвимости?
    - **Ответ**: Базовая проверка

11. **Дополнительно**: Навигация с кастомной логикой (каталоги и т.д.)
    - **Ответ**: Добавить `data-no-routing` атрибут. Если он есть на родителе — все дочерние элементы игнорируются роутером. Если на самой ссылке/кнопке — игнорируется при клике